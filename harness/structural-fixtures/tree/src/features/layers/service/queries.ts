// The target half of the layer-direction fixtures. Reached from above (legal),
// from below (a violation), and from beside it (legal), so that one file is what
// the three verdicts differ over rather than three files that might differ for
// some other reason.
export const listRows = () => ["row"];
