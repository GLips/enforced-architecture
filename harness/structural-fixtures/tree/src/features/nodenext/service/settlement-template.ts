// Imported for its TEXT, through `?raw`, by `settlement.ts` next door. Its own
// imports are what the query does not pull into a bundle — see the note in
// `module-resolution.ts` on why this check traces into it anyway.
export const TEMPLATE = "settlement";
