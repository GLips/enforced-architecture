// The type half of the sibling feature, so a crossing can be written as a
// type-only import without also being a value import of the same module.
export type BetaShape = { readonly beta: string };
