// LEGAL: the source `pricing.spec.ts` covers. Silent — it is not a test file.
//
// Here so the `.spec.` fixture is a mis-NAMED test rather than an orphan one.
// Delete this and that fixture starts firing for the wrong reason, and the
// off-convention branch goes unproven while the run stays green.
export const priceBands = ["free", "pro"];
