// WARNS test-file-mirror: `.spec` is off-convention here, and the extension is
// not what makes it so. The off-convention list is matched against the name with
// the extension already stripped — spelled with the extension in it, it governs
// the four extensions it names and clears the other four.
export const ledgerSpecPlaceholder = true;
