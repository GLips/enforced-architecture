// The sibling for `ledgers.test.mts`: a module written as an ES module, whose
// test is found only if the sibling lookup tries every source extension rather
// than the two the test itself could be written in.
export const ledgers = [] as const;
