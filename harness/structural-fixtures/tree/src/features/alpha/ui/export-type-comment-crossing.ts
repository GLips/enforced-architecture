// FIRES import-policy: the re-export spelling of the same loss. A comment
// between `export type` and the brace defeats the text rewrite, and a re-export
// is an import of the module it names.
export type /* re-exported for the ui layer */ { BetaShape } from "../../beta/service/beta-shape.ts";
