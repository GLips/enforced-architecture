// FIRES shadow-source: the JS key in an `.mts` module.
//
// The scan takes every source extension now rather than a configured list of
// three. A configured list is a list that can be shortened — to two, or to none
// — and a check that walks nothing reports clean. This file is what a shortened
// list stops reading.
export const panelStyle = { boxShadow: "0 1px 2px rgba(0, 0, 0, 0.2)" };
