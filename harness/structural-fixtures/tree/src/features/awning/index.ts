// ADVERSARIAL, and the contract's NEVER clause a second way: a boundary
// FABRICATED by JSX TEXT. `features/facade` fabricates one from a string, which
// masking string literals closes; this one is not in a string, so masking closes
// nothing. It is the fixture for the transpiler gate — the only thing that
// answers "is this module really imported" without a reader to out-spell.
export { AwningSetup } from "./controllers/settle.tsx";
