// Support for the two barrels above it: one server-side value and one
// client-safe one, so each barrel has something real of its own to expose.
export const dispatchPlanVersion = 2;

export const routeLabel = "dispatch";
