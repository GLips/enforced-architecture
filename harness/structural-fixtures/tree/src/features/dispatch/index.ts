// LEGAL: the CLIENT barrel, present and unused.
//
// It exists so `courier`'s import lands on the OTHER barrel by choice rather
// than by default. A grant covers the feature, not a barrel, and that claim is
// only testable where a feature has both.
export { routeLabel } from "./service/dispatch-plan.ts";
