// ADVERSARIAL: the shadow declared rather than parameterised — the framework
// import is genuine and unused, and a nested `const` of the same name is what
// the call reaches.
export { settlePelmet } from "./controllers/settle.ts";
