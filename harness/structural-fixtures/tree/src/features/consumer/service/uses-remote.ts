// LEGAL: a granted import of a feature the source walkers disagree about.
// The grant is real, non-empty, and has to be honoured.
import { remoteValue } from "@/features/remote/index.mts";

export const label = `remote:${remoteValue}`;
