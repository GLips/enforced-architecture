// LEGAL: the twin of `hidden` — same `.mts`-only shape, and it GRANTS its
// importer. Silent.
//
// `hidden` proves an unenumerated feature is denied; only this one proves the
// deny can be CLEARED. A rule that hard-codes "absent" for features its
// enumeration missed passes `hidden` and fails here, and the difference is the
// whole distance between a working rule and an unclearable blocking finding: the
// author reads the message, writes exactly the grant it asks for, and nothing
// changes because the file is never read.
export const remoteValue = "remote";
