// Infrastructure the layer-occupancy fixtures import. A schema module is query
// CONSTRUCTION capability: holding one is what lets a caller assemble its own
// query, which is the thing repo/ exists to concentrate.
export const invoicesTable = { name: "invoices", columns: ["id", "amount_cents"] };
