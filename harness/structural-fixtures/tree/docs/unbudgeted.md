# Scratch notes

Not listed in the manifest, and long enough that any check wandering past the
entries it was given would notice it. A doc nobody budgeted is a doc this check
has nothing to say about: the manifest is the subject, not the directory. If
that ever stops being true, this file starts reporting and the fixture that
proves the scope is the one that fails.

Padding follows, because the point of this file is that its size is irrelevant.
The manifest names what is governed. Everything else in this tree is free to be
any length at all, including this paragraph, which has no reason to end here and
no reason to keep going either.
