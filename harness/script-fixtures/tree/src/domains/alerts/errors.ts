// The hop that closes the transitive cycle, seen from the other end: this is
// what `thresholds/index.ts` imports. A leaf, so the ring is carried entirely by
// the domain graph and no two files import each other.
export class AlertError extends Error {}
