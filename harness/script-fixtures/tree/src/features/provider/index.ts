// LEGAL: a plain named re-export, so naming/barrel-discoverability stays quiet
// and this file's only job here is being a grantable public surface.
export { seatCount } from "./service/seat-count.ts";
