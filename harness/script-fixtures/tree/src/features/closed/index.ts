// LEGAL: a public surface with no visibility.json beside it. No file means no
// grants, so the default is deny — which is the whole default this rule sets.
export { rate } from "./service/rate.ts";
