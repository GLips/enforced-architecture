// LEGAL: the `.tsx` source of the pair below. Silent — it is not a test file.
//
// Holds no JSX deliberately. The extension is the entire point of this fixture,
// and a component here would drag the react/* checks into a case about file
// names.
export const receiptColumns = ["date", "amount"];
